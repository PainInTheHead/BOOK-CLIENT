import styled from "styled-components";

const Catalog = styled.div`
  margin: 0 auto;
  max-width: 1280px;
  height: 72px;
  .catalog-filter {
    display: flex;
    justify-content: space-between;
    align-items: center;
    h1 {
      font-size: 40px;
    }
  }
  .catalog-content {
  }
`;

export { Catalog };
